services:
  postgres-test:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: novel_agent_test
      POSTGRES_USER: novel_agent_test
      POSTGRES_PASSWORD: novel_agent_test
    ports:
      - "55432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U novel_agent_test -d novel_agent_test"]
      interval: 2s
      timeout: 2s
      retries: 30

  minio-test:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: novel_agent_test
      MINIO_ROOT_PASSWORD: novel_agent_test
    ports:
      - "9000:9000"
      - "9001:9001"
