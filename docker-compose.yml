version: '3.8'

services:
  # Ollama LLM service
  ollama:
    image: ollama/ollama:latest
    container_name: qunxiang-ollama
    volumes:
      - ollama-data:/root/.ollama
    ports:
      - "11434:11434"
    environment:
      - OLLAMA_HOST=0.0.0.0
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - qunxiang-net

  # PostgreSQL database
  postgres:
    image: postgres:15-alpine
    container_name: qunxiang-postgres
    environment:
      POSTGRES_DB: qunxiang
      POSTGRES_USER: qunxiang
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change_me_in_production}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U qunxiang -d qunxiang"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - qunxiang-net

  # pgBouncer connection pooler
  pgbouncer:
    image: edoburu/pgbouncer:latest
    container_name: qunxiang-pgbouncer
    environment:
      DATABASE_URL: "postgres://qunxiang:${POSTGRES_PASSWORD:-change_me_in_production}@postgres:5432/qunxiang"
      POOL_MODE: transaction
      MAX_CLIENT_CONN: "100"
      DEFAULT_POOL_SIZE: "20"
    ports:
      - "6432:5432"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - qunxiang-net

volumes:
  ollama-data:
    driver: local
  postgres-data:
    driver: local

networks:
  qunxiang-net:
    driver: bridge
