# PostgreSQL Connection (production - through pgBouncer)
DATABASE_URL=postgres://novel_agent:your_secure_password@localhost:6432/novel_agent

# Direct PostgreSQL connection (development only - bypasses pooler)
DATABASE_URL_DEV=postgres://novel_agent:your_secure_password@localhost:5432/novel_agent
